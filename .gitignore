__pycache__/
*.py[cod]
.venv/
.env
*.db
*.db-wal
*.db-shm
data/
processed/
error/
scan-in/
consume/
secrets/
.pytest_cache/
.ruff_cache/
graphify-out/
raw/
app/static/tailwind.css

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Token-Protokoll (nur lokal, nicht nach GitHub)
prd/token-log.csv

# Lokale NAS-spezifische Compose-Datei
.mynas.docker-compose.yml

# Arbeitsverzeichnis der Plan-Ausfuehrung (Ledger, Briefs, Review-Pakete)
.superpowers/

# OpenSpec-Geruest (generiert, pro Agent-Harness dupliziert). Nicht im Repo, damit
# push-image.sh einen sauberen Worktree sieht - es prueft `git status --porcelain`,
# und das zaehlt auch unversionierte Dateien als schmutzig.
.agents/
.opencode/
.claude/commands/
.claude/skills/
openspec/
